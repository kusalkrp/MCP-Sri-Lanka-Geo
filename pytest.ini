[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
# Disable unrelated installed plugins that trigger early app imports and
# interfere with the localhost URL patching in conftest.py
addopts = -p no:deepeval -p no:langsmith
